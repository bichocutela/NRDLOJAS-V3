name: nrdlojas_v3
description: NRD Lojas V3 - Flutter UI with Android Jetpack Compose native integration.
publish_to: "none"
version: 3.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
